## 函数与程序结构
### 函数
结构：
返回类型	函数名	（参数）
{
函数本体
}
eg:
一个计算两数之和的函数
```c
#include <stdio.h>
int sum(int x,int y)
{
	int he = x+y;
	return he;
}
int main()
{
	int x,y,he;
	x = 3;y = 5;
    he = sum(x,y);
    printf("%d+%d=%d\n",x,y,he);
    return 0;
}
```
运行结果3+5=8

一个没有返回值的函数：
```c
#include <stdio.h>

// 没有返回值，用 void
void printLine() {
    printf("----------------\n");
}

int main() {
    printf("开始\n");
    printLine();      // 调用函数
    printf("结束\n");
    return 0;
}
```
平均值：
```c
#include <stdio.h>

int add(int x,int y)
{
	int average= (x+y)/2;
	return average;
}

int main() {
   int a=8,b=6,c=0;
   c=add(a,b);
   printf("%d",c);
}
return 0 ;
```
### 程序结构
二、程序结构

1. 预处理指令

比如 #include <stdio.h>、#define    
特点是以 # 开头，末尾不加分号。主要作用是引入头文件、定义宏、条件编译等。

2. 全局变量声明，函数声明
写在所有函数（包括 main）外面的变量和函数声明。
int x //声明一个int类型的变量x
int add(int x,int y)
{

}
//函数声明，也叫函数原型，告诉编译器函数的名称、返回类型和参数列表。

· 全局变量声明：作用域从声明处到文件结束，整个程序都可以访问。

3. main函数

C程序的入口。程序运行时，第一个执行的就是 main 函数。
标准写法是 int main()，末尾 return 0; 表示程序正常结束。一个C程序必须有且仅有一个 main 函数。

4. 其他函数原型

除了 main 之外的函数声明（原型）。
函数原型的作用是让编译器提前知道有这个函数存在，这样即使在定义之前调用它，编译器也不会报错。

5. 函数内，局部变量 局部变量不初始化值是随机的

在函数内部定义的变量叫局部变量。
需要赋初值，如果不赋初值，它们的值是不确定的。所以在使用局部变量前，要先初始化，如 int a = 0;。

6. 语句都必须在函数内

C语言不是脚本语言，不能像Python那样在文件顶层直接写 printf("hello");。
所有可执行的语句，都必须写在函数体 { } 内。

7. C语言就是由函数组成的，语句不能凭空出现

C程序的基本单位是函数，整个程序就是一系列函数的集合。
main 是主函数，其他函数被 main 直接或间接调用。语句必须包裹在函数里，不能脱离函数单独存在。

8. C语言默认从main函数开始执行

不管 main 函数写在文件的最前面还是最后面，程序执行时，都是从 main 的第一条语句开始，然后依次调用其他函数，最后回到 main 并结束。

9. 将前面求最大值并返回地址的函数移动到别的C文件里，封装成.c文件，封装.h文件（第一个自己定义引用的.h文件）

这讲的是多文件编程（模块化）。
比如你写了一个求最大值并返回地址的函数 int* max(int*, int)，你不想把它和 main 放在一起，就可以：
把它放进一个 max.c 文件里（实现）
创建一个 max.h 文件，把函数声明放进去（接口）
在 main.c 里写上 #include "max.h"，就可以跨文件调用这个函数了。
这就是你自己定义并引用的第一个头文件。

10. c文件存放各类原型，h文件存放各类声明
.h 头文件：存放各类声明（函数声明、宏定义、结构体定义、外部变量声明）。
.c 源文件：存放各类实现（函数的具体代码、变量的定义）。
这样其他 .c 文件只要 #include 这个 .h 文件，就知道函数怎么用，而具体的代码隐藏在 .c 里。

11. 外部变量extern

extern 关键字用来声明一个变量是全局变量，表示这个变量是在别的 .c 文件里定义的。
比如你在 test1.c 里定义了 int count = 0;，想在 test2.c 里使用它，就在 test2.c 里写 extern int count;。
它的作用是扩展全局变量的作用域到其他文件。


