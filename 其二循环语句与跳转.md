## 循环结构

### 1. while循环
while（）//括号内的表达式为真，执行循环，否则不执行。
{
	
};
```c
#include <stdio.h>
int main()//主函数
{	
	int a=5;
	while (a)
	{
		printf("%d\n",a);
		a--;
		
	}
	printf("循环执行完了\n");
    return 0;
 }
```
当程序中a递减到0时while（）判定为假，停止循环。若（）中为常数，则while循环变为死循环。如：
```c
#include <stdio.h>
int main()//主函数
{	
	while (1)
	{
		printf("%d\n",a);
	}
	printf("循环执行完了\n");
    return 0;
 }
```
{}中的语句会反复被执行
关于while循环的例子
```c
#include <stdio.h>

int main() {
    int a = 0;
    int mima = 123456;  
    int count = 0;               
    int max_count = 3;           

    printf("请输入 6 位密码：");
    scanf("%d", &a);

    // 只要密码不对，且尝试次数没超过 3 次，就一直循环
    while (a != mima && count < max_count - 1)
    {
        count++;  // 尝试次数 +1
        printf("密码错误！你还有 %d 次机会。\n", max_count - count - 1);
        printf("请重新输入密码：");
        scanf("%d", &a);
    }

    // 循环结束后，判断是密码对了，还是次数用尽了
    if (a == mima) 
    {
        printf("密码正确！欢迎登录。\n");
    } else {
        printf("尝试次数已用尽。\n");
    }

    return 0;
}
```
### 2.do while循环
程序至少被执行一次，先执行do语句，再执行while循环。
do
{}
while();

对比：
// while：条件一开始就是假，一次都不执行
int a = 10;
while (a < 5) {
    printf("while循环\n");  // 不会打印
    a++;
}

// do-while：先执行一次，再判断条件
int b = 10;
do {
    printf("do-while循环\n");  // 会打印一次
    b++;
} while (b < 5);

### 3.for循环
for(表达式1;表达式2;表达式3;)
{};
```c
#include <stdio.h>
int main()//主函数
{	
    int i,j;
    for(i=5,j=3;i>0;i--)
	{
	printf("%d\n",i);
	break;//跳出当前循环
	}
	//跳转到这里
	return 0;
}
```
### 4.跳转语句
break://跳转到循环体的末尾之后执行
continue;
eg:
for(i=2;i<9;i++)
{}	if((9%3==0))
	
	printf("1");
	printf("2");
	continue;
	printf("3");
	printf("4");
	//continue跳转到这里
	
}
#### goto
```c
#include <stdio.h>
int main()//主函数
{	

	while（1）
	{
	//...代码
	goto b;
	//...代码
	}
	
	
	
	a：		//goto b会跳转到这里
	printf("这是a\n");
	b:
	printf("这是b\n");
	c:
	printf("这是c\n");
	
    return 0;
 }
```


